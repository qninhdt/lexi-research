[project]
name = "tau-research"
version = "0.1.0"
description = "Specialized Business Customer-Service Assistant via SFT to Agentic RL on tau3-bench Retail"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.12,<3.14"
authors = [{ name = "qninhdt", email = "qndt123@gmail.com" }]
dependencies = [
    "accelerate>=1.0",
    "datasets>=3",
    "jinja2>=3.1",
    "json-repair>=0.63.1",
    "peft>=0.14",
    "pydantic>=2.9",
    "pyyaml>=6",
    "rich>=13.0",
    "torch>=2.4.0",
    "transformers>=4.48",
    "trl>=0.12",
    "wandb>=0.18",
    "tau2",
]

[tool.uv.sources]
tau2 = { path = "third_party/tau2-bench", editable = true }

[project.optional-dependencies]
colab = [
    "bitsandbytes>=0.45",
    "flash-attn @ https://github.com/lesj0610/flash-attention/releases/download/v2.8.3-cu12-torch2.11/flash_attn-2.8.3%2Bcu12torch2.11cxx11abiTRUE-cp312-cp312-linux_x86_64.whl ; python_version == '3.12' and sys_platform == 'linux'",
    "vllm>=0.6.0",
]

[project.scripts]
tau-research = "tau_research.cli:main"

[dependency-groups]
dev = [
    "mypy>=1.11",
    "pytest>=8",
    "pytest-asyncio>=0.24",
    "ruff>=0.9",
    "types-PyYAML>=6",
]
smoke = [
    "accelerate>=1.0",
    "datasets>=3",
    "peft>=0.14",
    "torch>=2.4.0",
    "transformers>=4.48",
    "trl>=0.12",
]


[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
include = ["tau_research*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src", "."]
asyncio_mode = "auto"

[tool.ruff]
line-length = 100
target-version = "py312"
exclude = ["third_party"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[tool.mypy]
python_version = "3.12"
strict = true
disallow_subclassing_any = false
files = ["src/tau_research", "tests"]

[[tool.mypy.overrides]]
module = [
    "accelerate.*",
    "bitsandbytes.*",
    "datasets.*",
    "peft.*",
    "tokenizers.*",
    "torch.*",
    "tqdm.*",
    "transformers.*",
    "trl.*",
    "vllm.*",
    "wandb.*",
    "tau2.*",
]
ignore_missing_imports = true
follow_imports = "skip"
