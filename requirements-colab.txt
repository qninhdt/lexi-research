# Reproducible Colab stack for Qwen3.5's hybrid linear/full attention.
--index-url https://download.pytorch.org/whl/cu126
--extra-index-url https://pypi.org/simple

torch==2.7.1
torchvision==0.22.1
torchaudio==2.7.1
transformers==5.14.1
datasets
trl
peft==0.20.0
bitsandbytes==0.50.0
accelerate
wandb
pyarrow
ninja
packaging
psutil
pydantic
jinja2
openai
httpx
fastapi
uvicorn
# Fused CUDA kernels for Qwen3.5's GatedDeltaNet linear attention layers.
# Without these the model falls back to torch_chunk_gated_delta_rule which
# casts all tensors to FP32, consuming ~12 GiB extra activation memory and
# making gradient_checkpointing=false impossible on L4.
causal-conv1d>=1.6.0
flash-linear-attention
# Rust-backed parallel downloader for the Hub.
# Install current package in editable mode
-e .


